# SBTI 页面验收标准 — 基于 DESIGN.md

## 核心理念

**"新中式神话·双面国风"** — 每个视觉元素必须体现山海经文化内涵，不能是功能性UI堆砌。

---

## 一、视觉规范验收

### 1.1 字体规范

| 检查项 | 验收标准 | 自检 |
|--------|----------|------|
| 精怪名称 | 使用 `font-family: 'Playfair Display', serif` 或等效 class `font-display` | [ ] |
| 戳心文案 | 使用 `font-classic` class，字体为 Ma Shan Zheng | [ ] |
| 正文/选项 | 使用 Noto Sans SC，字重 400/500/700 | [ ] |
| 进度/数据 | 使用 JetBrains Mono 等宽字体 | [ ] |

### 1.2 颜色规范

**亮色模式（墨韵新生）**
- 背景主色：`--bg-primary: #f5f3ef` (浅云白) ✓
- 卡片背景：`--bg-elevated: #ffffff` ✓
- 文字主色：`--text-primary: #1a1a1a` ✓
- 文字次要：`--text-secondary: #4a4a4a` ✓
- 强调金色：`--accent-gold: #c9a962` ✓
- 主强调色：`--accent-primary: #2d4a3e` (青墨) ✓

**暗色模式（异兽觉醒）**
- 背景主色：`--bg-primary: #0d0d0d` (深渊黑) ✓
- 卡片背景：`--bg-elevated: #242428` ✓
- 文字主色：`--text-primary: #f5f3ef` ✓
- 强调金色：`--accent-gold: #d4af37` ✓
- 主强调色：`--accent-primary: #3a5a8a` (幽蓝) ✓

**36种精怪专属色** — 每个精怪卡片必须使用其专属色系（龙系/凤系/兽系/狐系/水系/其他/隐藏款）

### 1.3 背景规范

| 检查项 | 验收标准 |
|--------|----------|
| 云纹背景 | 背景必须有淡淡云纹纹理（可用CSS渐变/ SVG背景图） |
| 精怪轮廓 | 可选：背景有淡化的山海经精怪线条轮廓装饰 |
| 层次感 | 背景有轻微的渐变或纹理，不能是纯色平板 |

### 1.4 卡片规范

**基础卡片 `.sbti-card`**
- 背景：毛玻璃效果 `backdrop-filter: blur(16px)`
- 边框：1px solid var(--border)
- 圆角：1rem (16px)
- 阴影：var(--shadow)
- hover效果：发光 + 阴影增强

**隐藏款卡片 `.sbti-card-secret`**
- 边框：2px dashed var(--accent-danger)
- 右上角：🔒 隐藏款标签
- 暗色模式：发光效果 var(--secret-glow)

**精怪专属色条**
- 卡片顶部有4px渐变条，使用精怪专属色
- 通过 CSS `::before` 实现

### 1.5 间距规范

- 基础单位：8px
- 卡片内边距：1.5rem (24px)
- 选项间距：12px
- 元素间间距：16px / 24px / 32px

---

## 二、交互规范验收

### 2.1 进度条

| 检查项 | 验收标准 |
|--------|----------|
| 外观 | 高度6px，圆角3px |
| 填充色 | 从 --accent-primary 到 --accent-gold 渐变 |
| 过渡动画 | 宽度变化使用 300ms ease-out |
| 显示文字 | 显示 "N / 24" 和 "N%" |

### 2.2 选项按钮 `.sbti-option`

**默认状态**
- 背景：var(--bg-elevated)
- 边框：2px solid var(--border)
- 圆角：12px

**Hover状态**
- 边框：2px solid var(--accent-primary)
- 背景：var(--bg-secondary)
- 有轻微上浮 transform: translateY(-1px)

**选中状态**
- 边框：2px solid var(--accent-gold)
- 背景：var(--bg-secondary)
- 选项key背景：var(--accent-gold)，文字变白

**动效**
- hover/active 过渡：150ms ease
- 点击时有轻微 scale(0.98) 按压反馈

### 2.3 主题切换

- Header有暗色/亮色模式切换按钮
- 切换时有 300ms 平滑过渡
- 图标：Sun (亮色模式) / Moon (暗色模式)

### 2.4 页面过渡

| 场景 | 动画 | 时长 |
|------|------|------|
| 卡片入场 | fade + scale(0.95→1) | 400ms |
| 选项出现 | fade-in stagger | 50ms per item |
| 结果揭晓 | 异兽觉醒效果（暗色） | 600ms |
| 隐藏款解锁 | 粒子爆发 + 震动 | 800ms |

---

## 三、组件验收

### 3.1 Header

- 左侧：返回按钮 (ArrowLeft)
- 中间：标题 "山海经老板测试"
- 右侧：主题切换按钮
- 背景：毛玻璃效果 + 底部边框
- 高度：64px (py-3 + header)

### 3.2 问题卡片

```
┌─────────────────────────────────────┐
│ [题号]  题目文字内容                 │  ← 题号用accent-gold背景
│                                     │
│ ┌─────────────────────────────┐     │
│ │ [A]  选项文字                │     │  ← 选项完整展示
│ └─────────────────────────────┘     │
│ ┌─────────────────────────────┐     │
│ │ [B]  选项文字                │     │
│ └─────────────────────────────┘     │
│           ...                      │
└─────────────────────────────────────┘
```

### 3.3 结果卡片

```
┌─────────────────────────────────────┐
│ ▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔ │  ← 4px精怪专属色渐变条
│                                     │
│     🐉 (emoji)                      │  ← 精怪emoji 48px
│     貔貅 (name)                     │  ← Playfair Display, gold色
│     铁公鸡里的战斗机                 │  ← 称号, muted色
│                                     │
│ ┌─────────────────────────────┐     │
│ │ "你赚的每分钱都是..."        │     │  ← Ma Shan Zheng, 戳心文案
│ └─────────────────────────────┘     │
│                                     │
│ [💰只进不出] [👐事必躬亲] [⚖️稳扎稳打]│  ← sbti-tag
│                                     │
│ 赚钱潜力 ★★★★★                     │
│                                     │
└─────────────────────────────────────┘
```

### 3.4 分享解锁卡片

- 背景：渐变 var(--progress-gradient)
- 进度条：白色底 + 绿色填充 + 动画
- 三个分享按钮：吐槽风/励志风/商务风
- 复制成功：按钮变绿色 + CheckCircle2 图标

---

## 四、动效规范验收

### 4.1 CSS 类使用检查

| 类名 | 用途 | 必须使用 |
|------|------|---------|
| `.glass` | 毛玻璃效果 | [ ] |
| `.animate-card` | 卡片入场动画 | [ ] |
| `.animate-fade-in` | 淡入动画 | [ ] |
| `.animate-scale-in` | 缩放动画 | [ ] |
| `.animate-glow` | 发光动画 | [ ] |
| `.hover-lift` | 上浮效果 | [ ] |
| `.focus-visible-ring` | 键盘焦点环 | [ ] |
| `.font-classic` | 戳心文案字体 | [ ] |
| `.personality-name` | 精怪名样式 | [ ] |
| `.personality-accent` | 精怪专属色卡 | [ ] |

### 4.2 动画时序

| 动画 | 时长 | 缓动函数 |
|------|------|----------|
| 微交互 (hover/active) | 100-150ms | ease |
| 标准过渡 (展开/淡入) | 200ms | ease-out |
| 页面级动画 | 300-400ms | cubic-bezier(0.16, 1, 0.3, 1) |
| 异兽觉醒效果 | 600ms | ease-in-out |

---

## 五、响应式规范验收

- 最大宽度：480px (答题流程)
- 移动端优先，断点 640px / 768px / 1024px
- 字体最小 16px (防止 iOS 自动缩放)
- 触摸目标最小 44x44px

---

## 六、验收流程

### 自检清单 (每次提交前)

```
□ 所有 CSS 变量使用了 DESIGN.md 定义的值
□ 所有字体使用了规定的 font-family
□ 背景有云纹/纹理，不是纯色
□ 卡片有毛玻璃效果
□ 选项有 hover/selected 状态变化
□ 进度条有渐变填充和动画
□ 动效使用了定义的 CSS 类
□ 暗色模式完全不同的视觉风格
□ 无障碍：focus-visible-ring / aria-label
□ 触摸目标 ≥ 44px
```

### 测试方法

1. **视觉检查**：对比 screenshot 和 DESIGN.md 设计稿
2. **功能检查**：完成24题测试，验证结果卡片
3. **动效检查**：观察卡片入场、选项选择、结果揭晓动画
4. **主题检查**：切换暗色模式，验证"异兽觉醒"视觉效果
5. **响应式检查**：使用 DevTools 模拟不同设备

---

## 七、验收标准分级

### CRITICAL (必须通过)

- [ ] 精怪名称使用金色 (accent-gold)
- [ ] 戳心文案使用 Ma Shan Zheng 字体
- [ ] 暗色模式背景是深渊黑 (#0d0d0d)
- [ ] 选项 hover 有视觉反馈
- [ ] 进度条有渐变填充

### HIGH (强烈建议)

- [ ] 背景有云纹纹理
- [ ] 卡片有毛玻璃效果
- [ ] 有入场/揭晓动画
- [ ] 精怪专属色应用在卡片上

### MEDIUM (应该做到)

- [ ] 所有动效时长符合规范
- [ ] 分享按钮有复制成功反馈
- [ ] 响应式布局正常

---

## 验收通过条件

**CRITICAL 全部通过 + HIGH 80% 通过 = 可以合并**

---

*最后更新：2026-04-22*